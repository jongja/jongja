let month = 3
let toDo = "Travel"

var toDoArray = ["Travel", "Work", "Call"]
var evenNumber = [2, 4, 6, 8]
//var evenNumber: Array<Int> = [2, 4, 6, 8] //original version
//메모리에 연속적으로 저장되는 것이 아니다.

toDoArray[0] = "jongja"
toDoArray[1]
toDoArray[2]

toDoArray.append("may")
evenNumber.insert(12, at: 0)
evenNumber.remove(at: 0)
print(evenNumber)

for i in toDoArray {
    print(i)
}
